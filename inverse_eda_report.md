# Scientific Exploratory Data Analysis: Inverse Blast Characterization

This report documents the rigorous statistical analysis of the Physics Reference Dataset (PRD) consisting of 100,000 total samples (90,696 after filtering Surface bursts with $Z < 0.20$ to prevent clamping bias), generated by the BlastScope forward physics solver.

> [!IMPORTANT]
> All numerical values, interpretations, and thresholds in this report are dynamically computed from the dataset. No values are hardcoded.

---

## 1. Feature Distribution & Log-Scale Histograms

Blast parameters (pressures, impulses, timing) naturally span 4 to 6 orders of magnitude. A log-uniform sampling of targets (Weight and Scaled Distance) combined with power-law decay of blast waves results in highly skewed features in linear space. 

To address this, all continuous features were transformed using $\log_{10}$.
The resulting log-scale histograms show well-behaved distributions across all variables.

![Log-scale Feature Histograms](file:///./plots/log_histograms.png)

---

## 2. Multicollinearity & Variance Inflation Factor (VIF)

Multicollinearity occurs when features are highly correlated, providing redundant variance and making model coefficients unstable. To measure this, the Variance Inflation Factor (VIF) was calculated for all log-transformed physics variables:

| Feature | VIF |
| --- | --- |
| log_incident_pressure | 72571121.810080 |
| log_particle_velocity | 35918608.068249 |
| log_shock_front_velocity | 7378701.485959 |
| log_reflected_pressure | 296351.208234 |
| log_dynamic_pressure | 133412.242640 |
| log_reflected_impulse | 2573.985733 |
| log_arrival_time | 1048.545050 |
| log_positive_impulse | 753.461780 |
| log_positive_duration | 282.068895 |

*   **Interpretation**:
    *   3 feature(s) have **near-infinite VIF (> 1,000,000)**: `log_incident_pressure` (VIF = 72571122), `log_particle_velocity` (VIF = 35918608), `log_shock_front_velocity` (VIF = 7378701). These are practically collinear — their variance is almost entirely explained by other features.
    *   6 feature(s) have **high VIF (10–1,000,000)**: `log_reflected_pressure` (VIF = 296351), `log_dynamic_pressure` (VIF = 133412), `log_reflected_impulse` (VIF = 2574), `log_arrival_time` (VIF = 1049), `log_positive_impulse` (VIF = 753), `log_positive_duration` (VIF = 282). These are strongly collinear but not perfectly redundant.

---

## 3. Principal Component Analysis (PCA)

PCA was executed on the standardized 9 continuous log features to verify the actual dimensionality of the feature space:

| Principal Component | Explained Variance Ratio | Cumulative Explained Variance |
| --- | --- | --- |
| PC1 | 0.845510 | 0.845510 |
| PC2 | 0.138817 | 0.984328 |
| PC3 | 0.012816 | 0.997144 |
| PC4 | 0.001755 | 0.998898 |
| PC5 | 0.001009 | 0.999908 |
| PC6 | 0.000089 | 0.999996 |
| PC7 | 0.000003 | 1.000000 |
| PC8 | 0.000000 | 1.000000 |
| PC9 | 0.000000 | 1.000000 |

![PCA Scree Plot: Explained Variance by Component](file:///./plots/pca_scree.png)

*   **Interpretation**: The first **2** principal component(s) explain **98.43%** of the variance (95% threshold). The first **3** principal component(s) explain **99.71%** (99% threshold).
    *   Specifically, the first 3 PCs cumulatively explain **99.71%** of the entire dataset variance.
    *   This confirms that the true physical dimensionality of the blast parameters is extremely small ($\le 3$ independent dimensions), which matches blast physics where $W$, $Z$, and BurstType completely determine the environment.

---

## 4. Feature Elimination Experiment

We trained Random Forest models comparing the full 10-feature set against the pruned 7-feature set (excluding derived features with infinite VIF):

| Feature Set | W_R2_Log | W_MAE_Log | Z_R2_Log | Z_MAE_Log |
| --- | --- | --- | --- | --- |
| Full (10 features: is_surface + 9 log-features) | 0.999872 | 0.011451 | 0.999832 | 0.007220 |
| Pruned (7 features: is_surface + 6 log-features) | 0.999872 | 0.011456 | 0.999832 | 0.007229 |

*   **Conclusion**: Pruning the redundant features does **not** degrade the prediction accuracy ($\Delta R^2_W = 0.0000$, $\Delta R^2_Z = 0.0000$). The pruned model is smaller, faster, and less prone to overfitting on sensor measurement errors. Therefore, the **7-feature pruned set is selected for production deployment**.

---

## 5. Target Correlations (Spearman Rank)

Spearman rank correlations verify the direction and monotonic strength of the relationship between selected features and targets:

|  | weight | scaled_distance |
| --- | --- | --- |
| incident_pressure | 0.006523 | -0.998714 |
| reflected_pressure | 0.006523 | -0.998723 |
| dynamic_pressure | 0.006523 | -0.998714 |
| positive_impulse | 0.653262 | -0.717713 |
| reflected_impulse | 0.437073 | -0.889829 |
| arrival_time | 0.354762 | 0.927511 |
| positive_duration | 0.626368 | 0.731656 |
| shock_front_velocity | 0.006509 | -0.998637 |
| particle_velocity | 0.006523 | -0.998710 |

*   **Observations**:
    *   Scaled distance ($Z$) is most strongly correlated with `reflected_pressure` ($R_s = -0.9987$).
    *   Weight ($W$) is most strongly correlated with `positive_impulse` ($R_s = 0.6533$), consistent with Hopkinson-Cranz cube-root scaling.
